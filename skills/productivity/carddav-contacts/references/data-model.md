# Data model (planned)

Status: placeholder. Not yet implemented.

This document will define the normative `carddav-source/1.0` payload
schema and the `carddav-command/1.0` envelope: field shapes, closed key
sets, sort/canonicalization rules, and the generation-hash algorithm.

All examples in this file must use synthetic placeholder contacts only
(for example `Example Person`, `person@example.invalid`) — never real
contact data.
